2026-05-26: The `tmdb-platform-mapping` GitHub Actions job failed because the repo-owned mapping agent runs TypeScript source that imports scraper-kit subpath exports resolved from `dist`, but CI had not built `@forward-widget/scraper-kit`. Fixed by adding a gated workflow step to run `pnpm --filter @forward-widget/scraper-kit build` before the mapping agent.
2026-05-26: The follow-up CI run reached the new scraper-kit build step, but declaration generation failed because scraper-kit's own workspace dependencies (`@forward-widget/libs-fetch`, `@forward-widget/libs-storage`) had not produced types yet. Updated the workflow build step to `pnpm --filter @forward-widget/scraper-kit... build` so pnpm builds the dependency chain first.
