## 2026-05-17 Session start
- CLI contract is `tmdb:mapping-agent -- --issue <number> --issue-body-file <path> --summary-file <path>`.
- Ambiguous/invalid extraction writes a summary and exits `2`; workflow comments safely and creates no PR.
